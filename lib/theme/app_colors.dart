import 'package:flutter/material.dart';

class AppColors {
  // Tông màu chính (Primary) - Thể hiện sự trẻ trung, hiện đại
  static const Color purplePrimary = Color(0xFF6A1B9A); // Tím đậm (AppBar, Nút bấm)

  // Tông màu nền (Background)
  static const Color purpleLight = Color(0xFFF3E5F5);   // Tím cực nhẹ (Nền màn hình)

  // Tông màu hỗ trợ (Supporting)
  static const Color purpleSoft = Color(0xFFE1BEE7);    // Tím nhạt (Bong bóng chat của bạn)
  static const Color white = Colors.white;              // Trắng (Bong bóng chat người lạ)

  // Tông màu trạng thái (Status)
  static const Color greyText = Color(0xFF757575);      // Xám (Thời gian tin nhắn, Subtitle)
  static const Color errorRed = Color(0xFFD32F2F);      // Đỏ (Dùng khi văng lỗi hoặc Đăng xuất)
  static const Color successGreen = Color(0xFF388E3C);  // Xanh lá (Dùng cho thông báo thành công)
}